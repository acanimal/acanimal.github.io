version: '3'
services:
  site:
    command: jekyll serve --incremental --drafts --config _config.yml,_config-dev.yml
    image: jekyll/jekyll:latest
    volumes:
      - $PWD:/srv/jekyll
    ports:
      - 4000:4000

